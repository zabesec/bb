services:
  scanner:
    build: .
    container_name: shodan-dork
    environment:
      - COOKIE_URL=${COOKIE_URL}
      - TARGETS_URL=${TARGETS_URL}
      - DISCORD_WEBHOOK_URL=${DISCORD_WEBHOOK_URL}
    volumes:
      - ./output:/app/output
    restart: unless-stopped
